# Database (AWS RDS PostgreSQL)
# Serverless: use connection_limit=1 to avoid exhausting connections; optional connect_timeout.
# If using PgBouncer, use pool_mode=transaction and the same connection_limit.
DATABASE_URL="postgresql://username:password@endpoint:5432/pagerefresh?connection_limit=1&connect_timeout=10"

# AI APIs
ANTHROPIC_API_KEY="sk-ant-api03-..."
OPENAI_API_KEY="sk-..."

# Netlify
NETLIFY_BLOBS_TOKEN="..."

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Admin (MVP: single shared secret; gate for /admin and /api/admin)
ADMIN_SECRET="your-long-random-string-min-32-chars"

# Screenshot (local dev only - path to Chrome/Chromium)
# CHROME_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"

# Template import (optional - overrides built-in templates)
# When set, import-templates.ts reads markdown from this dir instead of templates.ts
# Brand-builder templates: .../web-design-and-development/brand-building-guides-and-examples/custom-section-feature-templates
# TEMPLATES_MD_DIR="/path/to/custom-section-feature-templates"
