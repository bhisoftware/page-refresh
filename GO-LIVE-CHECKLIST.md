# Go-Live Checklist

## Stripe
- [ ] Enable receipt emails: **Stripe Dashboard → Settings → Emails → Successful payments**
  - Sends a Stripe-branded receipt to the customer after every successful payment
  - Works automatically with Checkout Sessions (uses the email collected during checkout)
  - Custom domain branding can be configured in Stripe Dashboard → Settings → Branding → Emails
